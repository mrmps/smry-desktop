{
  "name": "smry-desktop",
  "private": true,
  "scripts": {
    "desktop:build": "bun scripts/build-desktop-app.ts",
    "desktop:notarize": "bun scripts/notarize-macos-release.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "pake-cli": "3.15.1"
  }
}
